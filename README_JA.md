# job-hunt：実際の経験を、応募書類の確かな根拠に

<p align="center">
  <a href="README.md">简体中文</a> ·
  <a href="README_EN.md">English</a> ·
  <a href="README_JA.md"><strong>日本語</strong></a> ·
  <a href="README_KO.md">한국어</a> ·
  <a href="README_ES.md">Español</a>
</p>

<p align="center">
  <a href="LICENSE"><img alt="ライセンス：MIT" src="https://img.shields.io/badge/License-MIT-yellow.svg"></a>
  <img alt="Claude Code と Codex 向け" src="https://img.shields.io/badge/agents-Claude_Code_·_Codex-5b5bd6">
  <a href="https://github.com/addsumtech/job-hunt/releases"><img alt="バージョン：v1.0.0" src="https://img.shields.io/badge/release-v1.0.0-1f883d"></a>
</p>

<p align="center">
  <img src="docs/assets/hero.jpg" alt="CV の各記述が、論文、ノート、証明書、プロジェクトなどの根拠につながっている図">
</p>

**求人を探し、応募するかを判断し、書類を整え、面接を練習する。経験を具体的に伝えながら、まだ満たしていない条件も明らかにします。**

job-hunt は Claude Code や Codex で使う求職支援 Skill です。希望、CV、求人情報を渡すと、エージェントが資料の読解、根拠の確認、文章の調整、組版、レビューを行い、Python スクリプトが出典、ファイルの整合性、成果物の引き渡し条件をチェックします。技術職や研究職を含むさまざまな職種に加え、キャリアチェンジ、離職期間、新卒、海外への応募などの状況も扱います。

目指すのは、書類のどの一行を聞かれても、自分の経験から説明できる状態です。

## 必要なステップから始める

| モード | 使う場面 | 主な成果物 |
|---|---|---|
| **discover · 求人を探す** | 方向性が決まり、検討する求人を見つけたい | 実在する求人の出典、取得状況、暫定評価を添えた候補一覧 |
| **assess · 応募を検討する** | 気になる求人があり、時間をかける価値を判断したい | 要件と根拠の対応表、必須の応募条件、不足点、応募に関する助言 |
| **apply · 書類を準備する** | 応募先を決め、その求人に合わせた書類が必要 | 調整済み CV、必要に応じたカバーレター、レビュー記録、面接準備メモ |
| **interview · 模擬面接** | 求人と CV があり、回答を練習して振り返りたい | 模擬面接一回分、逐語記録、独立した二段階の評価、確認事項 |

各モードは個別に使えます。一回の作業が終わると次の選択肢が提示され、続けるかはあなたが決めます。求人一覧から大量の CV が自動生成されることはありません。完成した書類を提出するかどうかの判断と提出操作も、あなたが行います。

インストール後は、例えば次のように依頼できます。

```text
job-hunt を使って、オランダの MRI 画像再構成に関する求人を探してください。
job-hunt を使って、この求人リンクと私の CV から、応募を検討する価値があるか評価してください。
job-hunt を使って、この求人に合わせて CV を調整し、カバーレターを書いてください。
job-hunt を使って、この求人と私の CV に基づく技術面接の練習をしてください。
```

エージェントは最初にモードと応募先の地域を確認し、必要な希望条件や資料を尋ねます。地域はあなたが確認するもので、過去の勤務地や会話の言語から推測しません。

## 経験を根拠のある応募書類にする仕組み

**記述ごとに根拠を残します。** 新しく加えるスキル、ツール、担当範囲、成果には、元のプロフィールの具体的な箇所、そのセッションでのあなたの回答、または実際に読み込んだ本人の論文やプロジェクトが必要です。既存の経験は並べ替えたり、強調したり、表現を変えたりできます。根拠がない内容は不足点の一覧に残します。対応関係を `claims.yaml` に記録し、書類の調整にはプロフィールのコピーを使って原本を保ちます。

**評価は要件ごとに根拠を示します。** 必須要件を、十分な根拠あり、一部の根拠あり、根拠なしに分け、就労資格や免許などの必須条件を先に示します。資料が不足している場合は `insufficient_evidence` として、何が足りないかを説明します。面接や内定の確率、根拠のない 0–100 の適合スコアを作ることは禁止しており、予測表現のチェックは、CV の九つの対応言語で定義済みの語句を検出します。ATS のキーワード網羅率は文章内の語句を測るもので、採用確率ではありません。

**通常の CV は三つの独立した AI レビューを受けます。** ATS 役はキーワードと解析しやすさ、採用担当者役は読みやすさと基本的な応募条件、採用部門の責任者役は経験、担当範囲、信頼性を確認します。それぞれ独立したコンテキストで実行し、三者とも `PASS` の場合に内部レビュー通過とします。修正可能な問題は直して再レビューし、最大三回まで実施します。事実に忠実な修正では埋められない不足だけが残れば、理由を示して早めに終了します。

**面接の振り返りでは、実際に話した内容を検証します。** 一つの評価は回答自体を、もう一つは事実の出典を確認します。既にある経験の整理や曖昧な点の確認を助け、説明の裏付けが取れない CV の記述は修正リストに入れます。

## インストール

ローカルコマンドの実行とファイルの読み書きができるエージェント環境、および **Python 3.10+** が必要です。`npx` を使う場合は Node.js/npm も必要です。次の三つから一つを選んでください。

### 方法 1：`npx skills` でインストール

```bash
npx skills add addsumtech/job-hunt
```

表示される案内に従ってエージェントとインストール範囲を選びます。ユーザー全体へのインストールには `-g`、エージェントの指定には `-a claude-code` または `-a codex`、確認の省略には `-y` を使えます。リポジトリのルートが Skill 本体なので、スクリプトと参照ファイルも一緒にインストールします。

### 方法 2：Claude Code プラグインとしてインストール

Claude Code 内で実行します。

```text
/plugin marketplace add addsumtech/job-hunt
/plugin install job-hunt@job-hunt
/reload-plugins
```

呼び出しは `/job-hunt:job-hunt` です。マーケットプレイスを更新する場合は `/plugin marketplace update job-hunt` を実行します。手動コピーとプラグインを併用すると、同じ Skill が二つ表示される場合があります。

### 方法 3：クローンしてシンボリックリンクを作成

ソースを読んだり編集したりする場合に適しています。以下は Claude Code への登録例です。

```bash
git clone https://github.com/addsumtech/job-hunt.git
cd job-hunt
mkdir -p ~/.claude/skills
ln -s "$PWD" ~/.claude/skills/job-hunt
```

Codex では最後の二行の `~/.claude/skills` を `~/.codex/skills` に置き換えます。リンク先が既に存在する場合は、既存のインストールを確認してください。

### 初回利用：環境設定は Agent に任せる

Skill の導入後、**「job-hunt の環境を設定して作業を始めて。Chrome 拡張機能の読み込みは自分で行います」**と伝えてください。

Agent は既存の環境を再利用し、作業に必要な Python 依存関係、Node.js、OpenCLI、文書ツールを導入して、[OpenCLI 公式拡張機能](https://github.com/jackwener/opencli/releases)をダウンロード・展開します。ターミナルのコマンドをコピーする必要はありません。

**通常、手動で行う設定は Chrome 拡張機能の読み込みだけです。**

1. Agent が `chrome://extensions/` を開き、展開済みフォルダーのフルパスを示します。
2. 「デベロッパーモード」を有効にし、「パッケージ化されていない拡張機能を読み込む」で `manifest.json` が直接入っているフォルダーを選びます。ZIP は選びません。macOS では `Command + Shift + G` でパスを貼り付けられます。
3. 読み込み完了を伝えると、Agent が `opencli doctor` で拡張機能と接続を確認し、適用可能な互換性パッチを処理して元の作業を続けます。接続済みなら再読み込みは不要です。

読み込んだフォルダーは移動・削除しないでください。サイトへのログイン、CAPTCHA、本人によるシステム権限の確認が必要な場合は、その操作のみお願いします。Agent は先に自動で進められる準備を完了します。

`doctor.py` は実際の機能を確認し、`doctor.py --install` は Python パッケージのみ導入します。他のツールは Agent が[初回設定手順](references/agent-setup.md)（英語）に従って導入します。Word のみの出力や貼り付けた求人の評価には、不要な PDF・ブラウザーツールを導入しません。

### 互換性パッチとブラウザーでの代替操作

パッチは本 Skill に同梱され、対象サイトの初回読み取り前に Agent が適用します。ブラウザー拡張機能を読み込むだけでは適用されません。

**通常、パッチを手動で導入する必要はありません。** Indeed・51job の読み取り前に既知の問題を確認し、OpenCLI のバージョンとソースが一致する場合だけ、ローカルコピーに自動適用します。インストール済みの本体は変更しません。現在の対象は 1.8.7 で、別バージョンや独自の変更は上書きしません。「求人サイトの互換性パッチを確認して」「互換性パッチを元に戻して」と Agent に依頼できます。[確認・適用・復元の手順](references/opencli-compat.md)（英語）。

アダプターの非互換性が確認された場合、web-access で Chrome などのブラウザーを操作し、**求人サイト自身の検索欄**から検索して結果を読み取れます。Google 検索だけを指すものではなく、先にパッチを入れる必要もありません。ログイン、CAPTCHA、権限による制限にはユーザーの対応が必要で、ツールの切り替えで回避することはできません。

## 地域・プラットフォーム・言語

求人検索は `opencli` のアダプターを通じて行います。リポジトリの出典一覧には 51job、Indeed、LinkedIn、BOSS 直聘などが含まれ、ログインの要否、求人情報、面接体験談を区別しています。実際に取得できるかは実行時に確認します。Indeed アダプターは米国サイトに接続し、国別サイトの切り替えには対応していません。他国の求人は、その地域の情報源を優先します。[出典一覧](references/discovery-sources.md)と[利用ルール](references/source-policy.md)（英語）を参照してください。

中国市場では、大手民間企業、中小民間企業、国有企業、外資系企業などの希望も確認します。これは順位付けの条件であり、他の種類を黙って除外するものではありません。牛客と一亩三分地は面接体験や選考プロセスの参考に使い、掲示板の投稿を求人として一覧に追加しません。

ログインや人間による確認が必要な場合は、理由とブラウザーでの操作を案内し、そのサイトの取得を一時停止します。「完了したので続けて」と返信すると、それまでの記録を残したまま、回数を制限した新しい取得を開始してアクセスの回復を確認します。頻度制限や権限の問題は別途説明します。取得できない場合は、別の情報源、求人本文の貼り付け、取得済みの結果の確認を選べます。求人検索は読み取り専用で、メッセージ送信、オンラインプロフィールの編集、自動応募は行いません。

**応募先地域のルールと出力言語は別々に扱います。** CV レンダラーには、英語、オランダ語、ドイツ語、フランス語、スペイン語、イタリア語、中国語、日本語、韓国語の見出しと個人情報ラベルがあります。`discover` と `assess` の集計カード、必須の見出しと説明は、中国語、英語、日本語、韓国語、スペイン語の[報告用テンプレート](references/report-localization.md)で生成・検証できます。内部文書、診断メッセージ、出典付きの地域慣行カードには未翻訳の部分があります。PDF には出力言語に対応するフォントも必要です。

米国、英国、ドイツ、オランダ、中国の **5 つの市場慣行表に計 38 件の記録**を収録し、出典と見直し期限を付けています。期限を過ぎた項目には表示を付け、対応する市場データがない場合もその旨を伝えます。各項目の適用範囲に沿って読むための背景情報です。

<p align="center">
  <img src="docs/assets/personal-data.jpg" alt="リポジトリの出力例：米国向け CV は個人情報を省略し、ドイツ向け CV は提供された写真と生年月日を表示">
</p>

個人情報は対象地域のルールに従います。米国、カナダ、英国、アイルランド、オーストラリア、ニュージーランド向けの通常の CV では、写真と `contact.personal` の項目をレンダラーが非表示にし、処理内容を説明します。認識できない地域でも省略を標準とします。他の認識済み地域では、ルールに従って提供された情報を表示できます。上図はレンダラーの動作例であり、その国のすべての雇用主が同じ形式を求めるという意味ではありません。

## 受け取れる成果物

| 資料 | 形式と内容 |
|---|---|
| 求人に合わせた CV | Markdown、Word（`.docx`）、PDF。PDF の組版ソース `.tex` も保存 |
| カバーレター・志望動機書 | 必要に応じて作成。Markdown、Word、PDF に対応 |
| 日本の履歴書 | 専用レンダラーで Markdown プレビューと編集可能な `.docx` を作成。表形式の PDF は Word または LibreOffice から出力 |
| 条件別の応募理由・実績説明 | NHS や英国公務員などの応募向けに、条件ごとに根拠を整理し、語数制限をチェック |
| 評価・面接資料 | 求人候補一覧、適合評価、面接準備メモ、模擬面接記録、振り返り |

条件ごとに採点される応募では、雇用主の基準に沿って supporting statement をレビューします。通常の CV も必要な場合は、その CV に別途三者レビューを行います。日本の履歴書は記入事項の完全性を確認し、併用する職務経歴書などの文書は通常の CV の手順でレビューします。

相談ごとに質問への回答をまとめた PDF レポートを作成し、履歴書などと同じ `~/Downloads/<workspace-name>/` フォルダーに保存します。複数の段階でも同じ納品フォルダーを使います。レポートにはキャリア分析、根拠、確認が必要な事項を記載し、ツールの診断情報は内部に残します。カバーレターは必要に応じて提供し、模擬面接を自動で始めません。PDF の生成と検証が終わるまで納品は未完了です。必要に応じて[補足情報源](references/supplementary-sources.md)から公式サイト、ニュース、GitHub、公開の議論を調べます。小紅書・抖音のログイン状態を使う検索には、その相談での明示的な許可が必要です。

納品物は `简历/`（履歴書）と `报告/`（レポート）に分け、`简历.docx`、`简历.pdf`、`求职建议报告.pdf` などの名前を使います。ファイル名に企業名や内部タスク識別子を含めません。

## ファイルの保存先

元のプロフィール、応募別の作業領域、チェック記録は、標準で `~/.claude/job-profiles/` に保存します。Codex でも同じ場所を使います。別のルートディレクトリを使う場合は `JOBHUNT_PROFILES_ROOT` で指定できます。

一人につき `profile.zh.yaml`、`profile.en.yaml` のように言語別の元プロフィールを保持でき、従来の `profile.yaml` にも対応します。応募ごとに作業領域を分け、出典記録と `journal.jsonl` に確認の履歴を残します。全体構造は [REFERENCE.md](REFERENCE.md)（英語）を参照してください。

ファイルはローカルに保存されます。モデルへのリクエストやウェブアクセスは、利用するエージェントとサービス設定に依存します。

## 検証と関連資料

自動チェックは、根拠への参照、元プロフィールの保護、レビュー結果の解析、個人情報の扱い、組版、モード間の引き継ぎなどを対象にしています。

```bash
python3 -m pip install pytest
make check
make eval-lint
```

`make check` は Python テスト、移行時の内容保持チェック、市場慣行表のチェックを実行します。一部のテストは実行機の Skill インストールや外部ツールを確認するため、その環境を踏まえて結果を読む必要があります。**チェックに通っても、成果物の正しさは保証されません**。求職活動の成果を示すものでもありません。

[`evals/`](evals/README.md) には 20 の動作評価シナリオがあります。記録済みの第 2 回評価（2026-09-05/06）では、15 のシナリオについて Skill あり・なしを対にして実行しました。両群の違いを確認する 10 項目が、ベースラインの `FAIL` から Skill 使用時の `PASS` に変わりました。**各シナリオ・各群につき一回、n = 1** です。安定した成功率や採用結果の予測ではありません。未実施の確認や無効とされたシナリオ結果も、[評価記録](evals/iterations/iteration-2-with-skill.md)に掲載しています。

リポジトリには、2026-09-06 に三つの CV レビュー役を `codex exec` で実行した検証も記録されています。各ホストでの実行方法と検証範囲は[他のエージェントでの利用](references/portability.md)を参照してください。未検証のホストを、検証済みの連携先として扱いません。

- [SKILL.md](SKILL.md)：モードの振り分けと基本ルール。
- [REFERENCE.md](REFERENCE.md)：構成、作業領域、データ構造、スクリプトの使い方。
- [プロフィール例](assets/profile.example.yaml)と[記述の出典例](assets/claims.example.yaml)：構造化データの形式。
- [動作評価ガイド](evals/README.md)：評価方法と制約。

技術資料は現在、主に英語で記述されています。[MIT License](LICENSE) で公開しています。
