# 更新日志

记录各版本面向使用者的改进与修复。

## 1.1.1 — 2026-09-25

### 浏览器连接

- 已经授权过的浏览器不会再被误报为「等待授权」。此前浏览器启动较慢时，留给授权确认的等待时间在握手开始前就已耗尽，用户会被引导去确认一个永远不会出现的提示；现在等待时间从首次读到连接状态时开始计算。
- 页面等待的每个阶段至少检查一次。此前进入时剩余时间已用尽的阶段一次都不检查，网站实际返回的 403 会被静默丢弃。

### 维护

- 修复每周变异测试一直无法完成的问题：测试进程不再写入 Python 字节码缓存，避免源码还原后仍按变异版本执行，或变异版本按原版执行；任务按文件拆成三个并行作业，并与常规检查使用同一 Node 版本。停止时会附上失败测试的输出。
- 发布流程：版本标签只在 main 的 CI 通过后创建，推送后不再移动；发布后的修复进入下一个补丁版本。

## 1.1.0 — 2026-09-20

本版本包含 1.0.1 计划中的修复，以及随后逐模块评估中发现的回归修复。

### 岗位检索与证据校验

- 修复校招页面的筛选、分页和多步导航；支持从网站首页进入依赖页面状态的岗位目录，保留每一步的原始页面证据。
- 修复仅统计最终页面、遗漏中间岗位列表的问题。导航前检查剩余额度，导航中记录实际岗位行数与页数，超限或证据不完整时停止后续操作。
- 支持对未知岗位目录进行受限的页面结构诊断；未确认岗位计数的诊断结果不能作为已完成检索或空结果通过验收。
- 加强来源覆盖、逐条筛选和完整职位描述校验，防止未执行的检索轮次、未处理的岗位线索或仅有列表摘要的岗位被计为完成。
- 修复绝对工作区路径、跨行岗位信息和校招关键词判断造成的误判，并更新 Indeed 的地区、雇主链接、薪资字段及职位详情读取兼容性。
- 检索完成度检查现在按各招聘网站的实际字段识别岗位线索：BOSS 直聘用 `name`、Indeed 的标题为空、微信搜狗文章按整条链接区分，此前这些线索对检查不可见，未处理完的检索会被判为完成。
- 岗位身份改按链接最后一段判定，`/vacatures/2026/xxx` 这类带年份的地址不再把两个岗位并成一个；Workday、实习僧等以标题作地址的网站也能与岗位清单对上。
- 新增「不是岗位」和「相关但未入选」两种排除理由；后者要求该岗位的链接必须出现在报告里，避免相关岗位被悄悄丢掉。
- 更正文档：达到目标数量不代表检索完成；中断后新开一轮，必须先在原工作区登记这一轮再开始读取。
- 荷兰、德国、英国不再只剩 LinkedIn 一个来源。此前的区域表写着「禁用 Indeed」，但被禁的其实只是那个写死美国站的适配器，不是 Indeed 的当地站点；现在国家站（nl/de/uk.indeed.com）可经浏览器路由读取，并一并写明该站会拦自动访问，遇到 403 或验证页按「站点拒绝」处理，绝不能当成「这个国家没有岗位」。市场本地招聘站（如荷兰学术岗位的 AcademicTransfer）也走同一条路。
- 面经帖、招聘方视角的人才记录不会再变成岗位清单里的一行。这类来源有搜索命令、也能取到真实 id，此前能通过全部出处校验后以「岗位」身份进入清单；现在按各来源「哪些命令返回的是职位」判定，不符合的直接拒收，它们改以线索形式出现在报告里。
- 岗位卡片用「·」连接时不再被误判为查无出处。LinkedIn 自己就用这个分隔符（如 `Epona · Breda-Tilburg Area (Hybrid)`），此前逐字抄录的卡片会整轮报错。
- 更新 LinkedIn 的实测记录：搜索与职位详情均已实跑验证，并注明 `salary` 字段在 18 行卡片里有 17 行为空——按薪资筛选卡片会几乎筛空，薪资需从职位详情读取或留空。间歇性的导航失败按网络问题重试一次处理，不当作平台封禁而停用整个来源。

### 浏览器连接与人工验证

- 复用日常浏览器的已授权连接，改善首次连接、慢页面和延迟加载的诊断，减少已连接后仍提示重新授权的情况。
- 修复脚本、样式和隐藏内容触发验证码误报的问题；区分正常空结果、页面加载失败、HTTP 错误和实际访问限制。
- 遇到登录墙、验证码或拒绝访问时，提示具体网站与原网页地址，等待用户完成必要操作后再继续；同一来源的停止状态在不同工具间保持一致。
- 明确 OpenCLI、AnySearch 与日常浏览器 CDP 的使用路径，完善依赖准备和已知站点适配修复。
- 只读点击改为先看控件形态：按钮类控件一律拒绝，只有翻页和查看详情才放行，岗位标题链接照常可点。此前「立即沟通」（发消息给招聘方，无法撤回）、「投简历」、「同意并投递」以及荷兰语、德语、法语、日语、韩语的申请与登录按钮都可能被点到。
- 同时不再误拒含有相同字样的真实岗位名，如「注册会计师」「邮政投递员」「数据删除合规专员」「Registered Nurse」。

### 简历与求职报告

- 2026-09-20 补充修复：生成和交付 Word 时检查 Markdown、LaTeX 源码残留，覆盖表格、跨文本片段及页眉页脚；检测到后要求修正并重新导出，避免 `$8 \times \text{H200}$` 等源码进入正文。补充普通文本与 Word 原生公式的使用规则。
- 改善中英文 Word 简历的排版、实习分组、项目内容保留和单页空间使用；按用户提供的参考样式检查最终 Word 与 PDF。
- 改善报告中的岗位分组、目录链接、自然分页和完整性检查，分别明确投递建议与面试准备的内容要求。
- 求职报告聚焦岗位判断、实际优势、证据缺口和下一步行动；内部检索日志、工具故障和技术验收记录单独保存。
- 修复中文、日文、韩文及其他已支持语言的 PDF 字形、文本提取与格式校验问题，改善 Windows 文本路径和 PDF 渲染失败恢复。
- 完善五种语言 README 的安装说明、首次使用指引与交付示例。
- 交付前校验：已审阅的报告 PDF 必须确实来自当前的报告正文。此前改了正文却没重新生成 PDF，仍会交付出「PDF 说建议投递、文字版说暂不投递」这种自相矛盾的一份。
- 版式检查按真正印出来的文字计算页边距与页面填充度，行尾空格不再被判越界，不可见的填充字符也不再算作内容。
- 交付不再误拦真实的面试建议（如「本轮只练了技术面」），只拦测试用虚构履历的痕迹。
- 简历里的项目经历条目此前在三种格式中都不会印出来，技能分组的标题也直接显示成内部字段名；现在项目要点会完整出现，分组标题按可读文字显示。
- 求职报告的岗位目录不再把每个链接印两遍。此前「原链接」那一列会先输出一行不可点的文字、下面再跟一行重复的可点链接；同时公司名与职位之间没有分隔，「MarvelX AI」加「AI Engineer」会连成一串看不出边界。

### 使用说明

- 浏览器单步或多步导航现在必须通过 `scripts/run_tool.py` 提供 `--workspace`、`--site` 和 `--budget-plan`，以便从日志读取已用额度并核对各阶段岗位列表。
- 历史捕获与日志继续保留；缺少中间页面计数的历史导航证据，需要按当前规则重新验证，不能直接视为验收通过。

## 1.0.0 — 2026-09-06

- 首次版本发布，提供岗位发现、匹配评估、申请材料制作和模拟面试四种模式。
- 支持 Claude Code 插件、`npx skills` 和仓库克隆安装。
- 提供多语言申请材料生成、地区惯例检查，以及 ATS、招聘人员和用人经理三个独立评审视角。
- 通过脚本校验经历与声明的来源，禁止编造履历和预测面试或录用概率。
